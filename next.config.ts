import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Deshabilitar Turbopack si hay problemas persistentes
  // Descomenta las siguientes líneas si el error de Turbopack persiste:
  // experimental: {
  //   turbo: false,
  // },
};

export default nextConfig;
